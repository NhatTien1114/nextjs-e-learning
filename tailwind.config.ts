/** @type {import('tailwindcss').Config} */
export default {
    theme: {
        extend: {
            colors: {
                primary: "#2C5EAD",
            },
            fontFamily: {
                primary: ["var(--font-be-vietnam-pro)"],
                secondary: ["var(--font-manrope)"]
            }
        },
    },
    plugins: [],
};